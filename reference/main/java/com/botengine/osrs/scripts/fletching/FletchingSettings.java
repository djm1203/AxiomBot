package com.botengine.osrs.scripts.fletching;

import com.botengine.osrs.ui.ScriptSettings;

/** Persisted settings for FletchingScript. */
public class FletchingSettings extends ScriptSettings
{
    public boolean bankingMode = true;
}
