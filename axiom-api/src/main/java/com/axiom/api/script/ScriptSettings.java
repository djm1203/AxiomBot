package com.axiom.api.script;

/**
 * Base class for all per-script settings objects.
 * Each script defines a concrete subclass with its config fields.
 * Fields should have sensible defaults so scripts work on first run
 * without a saved profile.
 */
public abstract class ScriptSettings
{
}
