package com.botengine.osrs.scripts.alchemy;

import com.botengine.osrs.ui.ScriptSettings;

/** Persisted settings for AlchemyScript. */
public class AlchemySettings extends ScriptSettings
{
    public int     alchemyItemId  = 0;    // 0 = auto-detect
    public boolean bankingMode    = false;
}
